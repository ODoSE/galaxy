<tool id="divergence_filter_recombination" name="Find recombined orthologs">
    <description>by looking at values returned by PhiPack</description>
    <requirements>
        <requirement type="python-module">divergence</requirement>
    </requirements>
    <command interpreter="python">wrapper.py divergence.filter_recombination $logfile
--orthologs-zip $orthologszip
--stats-file $statsfile
</command>
    <inputs>
        <param format="orthologs.zip" name="orthologszip" type="data" label="Orthologous genes" />
    </inputs>
    <outputs>
        <data format="txt" name="logfile" label="Logfile for ${tool.name}" />
        <data format="tabular" name="statsfile" label="PhiPack values per ortholog"/>
    </outputs>
    <help>This tool allows you to filter out orthologs in that show evidence of recombination through PhiPack.</help>
</tool>
