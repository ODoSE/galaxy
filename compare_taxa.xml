<tool id="divergence_compare_taxa" name="Compare taxa">
    <description>from unfiltered and filtered orthologs</description>
    <requirements>
        <requirement type="python-module">divergence</requirement>
    </requirements>
    <command interpreter="python">wrapper.py divergence.compare_taxa $logfile
--unfiltered-taxon-a $unfiltereda
--unfiltered-taxon-b $unfilteredb
--filtered-taxon-a $filtereda
--filtered-taxon-b $filteredb
</command>
    <inputs>
		<param format="tabular" name="unfiltereda" type="data" label="Unfiltered Taxon A IDs" />
		<param format="tabular" name="unfilteredb" type="data" label="Unfiltered Taxon B IDs" />
		<param format="tabular" name="filtereda" type="data" label="Filtered Taxon A IDs" />
		<param format="tabular" name="filteredb" type="data" label="Filtered Taxon B IDs" />
    </inputs>
    <outputs>
        <data format="txt" name="logfile" label="Logfile for ${tool.name}" />
    </outputs>
    <help>
This tool concatenates orthologs per genome into a FASTA records, and combines the records into a single FASTA file.
From this FASTA file a phylogenetic tree is created and visualized, from which the taxa are deduced by looking at the
largest two clades. 
  </help>
</tool>
