<tool id="divergence_translate" name="Extract &amp; translate genes">
	<description>from GenBank files and translate to proteins</description>
	<requirements>
		<requirement type="python-module">divergence</requirement>
	</requirements>
	<command interpreter="python">wrapper.py divergence.translate $logfile
#if $genomes.dataset
--genomes $genomes
#end if
#if $externalzip.dataset
--external-zip $externalzip
#end if
--dna-zip $dnazip
--protein-zip $proteinzip
</command>
	<inputs>
		<param format="projectids" name="genomes" type="data" label="GenBank Project IDs" optional="True"/>
		<param format="genomeszip" name="externalzip" type="data" label="External Genomes" optional="True"/>
	</inputs>
	<outputs>
		<data format="txt" name="logfile" label="Logfile for ${tool.name}" />
		<data format="dnazip" name="dnazip" label="Extracted DNA sequences" />
		<data format="proteinzip" name="proteinzip" label="Translated gene proteins" />
	</outputs>
	<help>
This tool:

- downloads GenBank files for the GenBank Project IDs in the complete genomes table,

- extracts genes from GenBank files,

- translates genes to protein level according to the specified coding table,

- returns archives of extracted DNA and translated Protein files all in FASTA format.
</help>
</tool>
